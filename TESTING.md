# 童课 · 验收测试清单

## A. 自动化测试（已通过）

`npm test` — 35 个用例，覆盖：

| 模块 | 覆盖内容 |
|---|---|
| utils/date | 跨月/跨年、周一起始、月网格、时间回绕、非法日期（Safari 兼容） |
| utils/statistics | 本周/本月/累计口径、取消缺席不计入、跨月边界 |
| reminderService | 低课时/到期/已过期/合并提醒/排序 |
| classRecordService | **场景 3-10**：记课减课时、防重复、补录、删除恢复、初始课时记录、结课/回退、删除保护 |
| backupService | **场景 13-16**：导出→清空→导入完整恢复、非法数据零破坏、版本检查、孤儿记录跳过 |

## B. 手动回归清单（场景 1-2、17-21）

前置：`npm run dev` → 打开 http://localhost:5173 →「我的 → 关于 → 填充演示数据」

| # | 场景 | 步骤 | 预期 |
|---|---|---|---|
| 1 | 新增孩子 | 「我的 → 孩子管理 → 添加孩子」 | 保存后首页头部显示新孩子；多孩子时点名字可切换 |
| 2 | 新增课程 | 「课程 → 新增」填 50 节英语 | 列表出现课程卡片，剩余 50 节 |
| 3-5 | 一键记课 | 首页「今日课程」点「✓ 上完课」 | ≤5 秒完成；按钮变 ✓ 已完成；余额 -1；Toast 显示剩几节 |
| 6-7 | 防重复 | 同一时段再次点击（或双指连点） | 按钮立即禁用；无第二条记录；提示"该时段已经记录过了" |
| 8 | 手动补录 | 「记录 → ＋ 补录」 | 保存后列表出现；课时数变化正确 |
| 9-10 | 删除记录 | 记录列表点条目 → 删除这条记录 | 二次确认；课程已用课时自动恢复 |
| 11-12 | 统计 | 「统计」Tab | 本周/本月/累计数字与记录求和一致 |
| 13-16 | 备份恢复 | 「我的 → 数据备份」导出 → 清空 → 导入 | 导出下载 JSON；清空前自动下载备份；导入提示"数据恢复成功"，数据完整；故意导入损坏 JSON → 报错且现有数据无损 |
| 17 | 浏览器刷新 | F5 | 数据仍在 |
| 18 | 关闭重开 | 关闭标签页重新打开 | 数据仍在（IndexedDB 持久化） |
| 19 | 离线使用 | DevTools → Network → Offline 后刷新 | App 完整可用（部署版需已访问过一次） |
| 20 | iPhone Safari | 同一 WiFi：`npm run dev -- --host`，iPhone 访问局域网地址 | 触摸交互、日期选择器、BottomSheet 正常；无横向滚动；无输入聚焦缩放 |
| 21 | 添加到主屏幕 | iPhone Safari → 分享 → 添加到主屏幕；桌面 Chrome → 地址栏安装图标 | 桌面出现「童课」图标；点击全屏 standalone 运行；飞行模式可用 |

## C. 验收红线（必须全部满足）

1. **数据一致性**：任意操作后，「课程剩余课时」恒等于「总课时 − 已完成/补课记录课时之和」（可用「数据修复」按钮核验）
2. **导入安全**：导入失败时当前数据零破坏
3. **无横向滚动**：390px 宽度下所有页面
4. **触控达标**：所有可点元素 ≥ 44×44px
5. **日期格式**：所有日期输入输出为 YYYY-MM-DD，Safari 无 NaN
